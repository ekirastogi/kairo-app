{
  "name": "Groww Trader",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "user": "node",
  "install": "./scripts/cloud-agent-install.sh",
  "start": "./scripts/cloud-agent-start.sh",
  "terminals": [
    {
      "name": "frontend",
      "command": "cd frontend && npm start -- --host 0.0.0.0 --port 4200",
      "description": "Angular dev server on http://localhost:4200"
    },
    {
      "name": "backend",
      "command": "cd backend && (test -f .env && set -a && source .env && set +a || true) && go run .",
      "description": "Go trading worker + HTTP API on http://localhost:8080 (requires Supabase/Firebase secrets)"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 4200 },
    { "name": "backend-api", "port": 8080 }
  ]
}
